-- This placeholder file exists only to satisfy editors/extensions that still
-- expect a .sql file at this path.
--
-- The real PostgreSQL seed reference lives in:
--   roadresq_seed.psql

SELECT 1;
